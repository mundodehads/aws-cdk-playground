export const enum Environments {
	SANDBOX = 'SBX',
	DEVELOPMENT = 'DEV',
	STAGING = 'STG',
	PRODUCTION = 'PROD',
}

export const DEFAULT_ENVIRONMENT = Environments.SANDBOX

export const enum Regions {
	SANDBOX = 'us-east-1',
	DEVELOPMENT = 'us-east-1',
	STAGING = 'us-east-1',
	PRODUCTION = 'us-east-1',
}

export const DEFAULT_REGION = Regions.SANDBOX
